# DEBUGGING NOTES

이 파일은 발생한 이슈의 증상/원인/조치를 기록한다.

---

## [2026-07-20] npm install ENOTEMPTY 에러

- 증상: `node_modules\axobject-query\...` 등에서 `rmdir ENOTEMPTY`로 npm install 실패
- 원인 추정: 이전 install이 타임아웃으로 중간에 끊겨 node_modules가 불완전한 상태로 남음
- 조치: node_modules, package-lock.json 삭제 → `npm cache clean --force` → npm install 재실행

## [2026-07-20] 학생/관리자 라우트 stub 및 build 검증

- 증상: 학생 마이페이지/설정/스티커와 관리자 주요 페이지가 단순 안내 카드 수준으로 축소되어 앱 구조가 뒤섞여 보였고, 이전 복원 내용이 일부 누락됨.
- 원인: `3e44de5` 이후 라우트 파일이 삭제되거나 stub으로 재생성되면서, `61b9aa9`의 레이아웃/정책/store 기능 일부가 워크트리에 반영되지 않음.
- 조치: 학생 화면을 PRD 기준으로 홈·스티커·마이페이지·설정 역할에 맞게 재구성하고, 관리자 라우트/store/type을 이전 완성 커밋 기준으로 복원했다. `npm.cmd run build`로 Windows PowerShell 실행 정책을 우회해 production build 통과 확인.

## [2026-07-20] Supabase 패키지 설치 및 build trace 잠금

- 증상: `npm.cmd install @supabase/supabase-js` 최초 실행 시 사용자 npm cache 경로에서 `EPERM` 실패. 이후 `npm.cmd run build` 최초 실행 시 `.next/trace` open `EPERM` 실패.
- 원인: npm cache 접근 권한이 sandbox에 막혔고, 3001 dev 서버가 `.next/trace`를 잡고 있어 production build가 trace 파일을 열 수 없었음.
- 조치: npm install은 승인된 권한으로 재실행해 성공. 3001 dev 서버 프로세스를 종료한 뒤 `npm.cmd run build`를 재실행해 성공.

## [2026-07-20] 코드리뷰 중 unused/typecheck 위험 정리

- 증상: `npx.cmd tsc --noEmit --noUnusedLocals --noUnusedParameters` 실행 시 unused 변수/import/상수 6건이 `TS6133`으로 실패.
- 원인: 이전 UI 복원 과정에서 `todayAttendance`, `useState`, `Pill`, `useRef`, `computePeriodBounds`, `MEDAL_LABEL`이 남아 있었지만 실제 렌더링 경로에서 사용되지 않음.
- 조치: 사용하지 않는 선언을 제거하고 `npm.cmd run typecheck`, `npm.cmd run build`, 엄격 unused 체크를 모두 통과시킴.

## [2026-07-21] `/signup` query parameter build failure

- 증상: `/signup?invite=...` 통합 후 production build에서 `prerender-manifest.json`이 생성되지 않았습니다.
- 원인: client page의 `useSearchParams` 호출에 Suspense 경계가 없어 Next.js 정적 prerender가 완료되지 않았습니다.
- 조치: 회원가입 폼을 `Suspense`로 감싸고 typecheck 및 production manifest 생성을 다시 확인했습니다.

## [2026-07-21] Signup invalid request-path message

- 증상: 회원가입 시 비밀번호 입력 아래에 `Invalid path specified in request URL`이 표시되었습니다.
- 확인: Supabase URL 형식과 server-only service key 읽기 연결은 정상입니다.
- 조치: 이메일 확인 복귀 주소를 현재 앱의 `/login`으로 명시하고, 같은 Auth 오류는 Supabase Site URL 및 Redirect URLs 설정을 안내하는 문구로 교체했습니다.

## 2026-07-21 Supabase Hosted Auth password minimum

- 증상: 앱의 6자 미만 비밀번호 정책과 Supabase Hosted Auth의 가입 제한이 달라 회원가입이 거부될 수 있었다.
- 원인: Supabase Hosted Auth의 최소 비밀번호 길이는 6자 미만으로 설정할 수 없다.
- 조치: 로그인·회원가입 검증과 화면 안내를 최소 6자로 통일했다.

## 2026-07-21 한글 아이디와 자동 로그인

- 증상: 한글 아이디를 입력하면 브라우저가 이메일 형식 오류를 먼저 표시할 수 있었다.
- 원인: `type=email` 입력은 앱의 한글 아이디 변환 전 브라우저 기본 검증을 실행한다.
- 조치: 로그인·회원가입 식별자 입력을 `type=text`로 유지하고, 자동 로그인 체크 상태에 따라 Supabase 세션 저장소를 localStorage 또는 sessionStorage로 분리했다.

## 2026-07-21 Vercel 회원가입 리디렉션 URL

- 증상: `https://stickerup.vercel.app/signup` 이메일 가입에서 `Invalid path specified in request URL`가 발생했고, 앱이 Supabase URL 설정 오류로 잘못 안내했다.
- 원인: `window.location.origin`을 직접 조합한 `emailRedirectTo`에 유효성 검증·환경변수 보조값·생략 처리 경로가 없었다.
- 조치: 현재 브라우저 origin을 검증해 우선 사용하고 `NEXT_PUBLIC_SITE_URL`/`NEXT_PUBLIC_APP_URL`을 보조값으로 사용한다. 유효한 URL을 만들 수 없으면 `emailRedirectTo`를 보내지 않아 Supabase Site URL 기본값을 사용한다.

## 2026-07-21 Supabase service-role 가입 진단

- 증상: Vercel `SUPABASE_SERVICE_ROLE_KEY`가 없거나 잘못된 경우 가입 완료가 실패해도 원인을 명확히 알기 어려웠다.
- 원인: 관리자 client가 서버 route에서 생성되지만, 이메일 인증 시작 전 서버 키 상태를 확인하는 경로와 잘못된 키의 사용자용 오류 매핑이 없었다.
- 조치: server-only 관리자 상태 점검 route와 `/api/signup`에 누락·잘못된 키를 `503`과 안내 문구로 반환하는 처리를 추가했다. admin client import는 API route와 server-only auth helper에만 존재함을 확인했다.

## 2026-07-21 회원가입 인증 링크 재발

- 증상: origin 검증과 Vercel 배포 후에도 `Invalid path specified in request URL` 기반의 가입 인증 링크 오류가 계속 발생했다.
- 원인: 선택값인 `emailRedirectTo`가 Auth `signUp` 호출에 남아 있어 앱 코드가 인증 리디렉션 주소 처리 경로에 계속 개입했다.
- 조치: `emailRedirectTo`와 origin helper를 제거하고 Supabase Dashboard Site URL 기본 동작만 사용하도록 변경했다.

## 2026-07-21 Supabase 프로젝트 URL 경로 오류

- 증상: 한글 아이디 가입에서 인증 링크 오류 메시지가 반복됐다.
- 원인: `NEXT_PUBLIC_SUPABASE_URL`이 프로젝트 root가 아닌 `.../rest/v1/` endpoint였다. 한글 아이디 가입은 server-only admin Auth 요청을 사용하므로 SDK가 잘못된 경로를 조합했다.
- 조치: Supabase browser/admin client 생성 전에 URL을 HTTPS origin으로 정규화했다. Vercel 환경변수도 `/rest/v1/` 없는 프로젝트 root URL로 수정해야 한다.

## 2026-07-21 students 테이블 권한 거부

- 증상: 한글 아이디 가입 시 `permission denied for table students`가 표시됐다.
- 원인: 서버 전용 `service_role`에 마이그레이션으로 생성된 public 테이블 권한이 명시적으로 부여되지 않았다.
- 조치: `20260719_05_service_role_permissions.sql`에 service_role 권한 부여를 추가하고, 가입 전 admin 상태 점검과 API 오류를 SQL 실행 안내로 매핑했다. Supabase SQL Editor에서 migration 05 실행이 필요하다.

## 2026-07-21 학원 미등록 학생 가입

- 증상: 초대 링크 없이 학원명을 입력한 학생이 `Academy was not found` 오류를 받았다.
- 원인: 가입 로직이 기존 tenant만 허용해 pending 또는 미연결 학생 가입 요구사항과 맞지 않았다.
- 조치: 미등록 학원명에는 ownerless pending tenant와 pending enrollment를 생성하고, 동일 학원명 선생님 가입 시 그 tenant를 이어받도록 변경했다.

## 2026-07-21 가입 트리거 결과 미수신 오류

- 증상: 학생 가입 중 `query has no destination for result data`가 발생했다.
- 원인: 실제 Supabase DB의 기본반 생성 또는 학생 자동 배정 트리거 함수가 최신 정의와 달라 SELECT 결과가 처리되지 않았다.
- 조치: `20260719_06_repair_onboarding_triggers.sql`로 두 트리거 함수를 재적용하고, API에서 SQL Editor 실행 안내를 반환하도록 변경했다.

## [2026-07-21] End-of-day onboarding database handoff

- The repository-side signup error mapping and trigger repair migration are prepared. Production signup still requires the Supabase project to run `20260719_05_service_role_permissions.sql` and then `20260719_06_repair_onboarding_triggers.sql` when those migrations have not yet been applied.

## [2026-07-21] 배포 초대 링크 500 오류

- 증상: `/join/student-f78657dc436a`가 Vercel에서 HTTP 500을 반환하고, 대응하는 `/api/invites/...`는 404 invalid/expired를 반환했다.
- 원인: 조직 관리의 초대 링크 발급이 브라우저 상태에만 추가되어 Supabase `invite_links`에 저장되지 않았다. 서버 리디렉션 진입 페이지도 배포 환경에서 500을 반환했다.
- 조치: 인증된 `/api/admin/organization`에서 초대 링크를 조회·발급해 Supabase에 저장하도록 변경하고, `/join` 진입은 클라이언트 전환 방식으로 바꿨다. 기존에 임시 상태로 발급한 링크는 새 배포 후 재발급해야 한다.
- 추가 확인: 최신 배포에서도 모든 `/join/:token` 동적 경로가 500이지만 동일한 `/signup?invite=:token` 경로는 200이었다. Next.js 설정의 라우팅 단계에서 `/join`을 `/signup`으로 넘기고, 신규 링크도 `/signup` URL을 직접 사용하도록 변경했다.

## [2026-07-21] 초대 가입자 아이디 및 선생님 유형 초기화 오류

- 증상: 초대 가입 화면에 현재 로그인된 김정원 선생님의 아이디가 고정되고, `type=teacher` 초대도 학생 가입 유형으로 돌아갔다.
- 원인: 초대 화면에서도 기존 Supabase 세션 메타데이터를 폼에 주입해 아이디를 비활성화했고, 초대 조회 성공 처리에서 초대 역할을 설정한 직후 `student`를 다시 설정하는 중복 코드가 있었다.
- 조치: 초대 링크 진입 시 로컬 기존 세션을 종료하고 아이디·비밀번호를 빈 입력값으로 초기화했다. 중복 학생 유형 설정을 제거해 DB의 `inviteeRole`을 유지하고, 학원 이름은 초대 데이터의 읽기 전용 값으로 유지했다.

## [2026-07-21] 선생님 권한 수정 경계 누락

- 증상: 조직 관리에 권한 토글이 표시되지만 모두 비활성화되어 원장도 수정할 수 없었고, 역할을 서버에서 검증하는 권한 변경 API가 없었다.
- 원인: 초기 화면 구현이 권한 조회 UI까지만 연결되고 실제 저장 mutation과 owner 검증 경계가 구현되지 않았다.
- 조치: 동일 조직의 `owner`만 보조 선생님의 권한을 변경할 수 있는 PATCH API를 추가했다. 클라이언트도 원장에게만 토글을 활성화하고 보조 선생님 및 원장 본인 권한은 수정하지 못하도록 했다.

## [2026-07-21] 학생-선생님 연결 링크 500 오류

- 증상: 학생이 발급한 `/connect/student/{token}` 링크를 배포 환경에서 열면 HTTP 500이 발생했다.
- 원인: 앞서 `/join/{token}`에서도 500을 일으킨 동적 진입 페이지와 동일한 구조로 연결 화면이 구현되어 있었다.
- 조치: 연결 화면을 정적 `/connect/student?token=...` 경로로 추가하고 신규 링크는 해당 주소로 발급한다. 기존 `/connect/student/{token}` 주소는 Next.js 리디렉션으로 새 주소에 연결했다.
- 배포 확인: `https://stickerup.vercel.app/connect/student/connect-f78a530f7eaf4a3d`는 500, 같은 토큰의 연결 API는 200, 새 쿼리 화면은 404였다. 따라서 토큰/DB가 아니라 아직 수정 코드가 배포되지 않은 상태로 확인됐다.

## [2026-07-21] Vercel 랭킹 기간 함수 타입 오류

- 증상: Vercel 빌드에서 `app/admin/rewards/page.tsx`가 `computePeriodBounds`에 인자 5개를 전달했지만 함수는 1~3개만 받도록 정의되어 타입 검사가 실패했다.
- 원인: 커스텀 기간 UI 호출부와 공용 기간 계산 함수의 변경이 서로 다른 커밋에 포함되어 깨끗한 배포 체크아웃에서 시그니처가 일치하지 않았다.
- 조치: 공용 함수에 선택적 시작일·종료일 인자를 추가하고 관련 기간 타입까지 원격 커밋에 포함했다. 로컬 프로덕션 빌드와 실제 배포 페이지 HTTP 200을 확인했다.

## [2026-07-21] happia1 개발자 로그인 권한 차단

- 증상: `happia1` 비밀번호 인증은 성공했지만 후속 API 권한 검사에서 `개발자 계정만 접근할 수 있습니다.`가 표시됐다.
- 원인: 단일 고정 개발자 계정도 Vercel 환경변수 허용 목록과 다시 대조해, 환경변수 적용 범위나 재배포 시점이 맞지 않으면 정상 계정이 차단됐다.
- 조치: `happia1`의 고정 내부 Auth 이메일을 기본 개발자 계정으로 서버에서 직접 인정했다. 비밀번호 검증은 계속 Supabase Auth가 담당하며 환경변수는 추가 개발자 계정을 허용할 때만 사용한다.

## [2026-07-21] 일반 회원가입에 기존 개발자 계정이 고정되는 오류

- 증상: 개발자 로그인 후 일반 회원가입으로 이동하면 내부 Auth 이메일과 기존 이름·학원 정보가 입력란에 고정되고 비밀번호를 새로 입력할 수 없었다.
- 원인: `/signup`이 모든 기존 Supabase 세션을 이메일 인증 후 가입 재개 세션으로 간주해 메타데이터를 폼에 주입했다.
- 조치: `resume=1`로 명시된 가입 재개 요청에서만 기존 세션을 사용한다. 일반 및 신규 초대 회원가입은 로컬 세션을 종료하고 아이디·비밀번호·이름·나이·학원명을 빈 값으로 초기화한다.

## [2026-07-21] 로컬 Next 빌드 캐시 파일 점유

- 증상: 상품 카탈로그 변경 검증 중 빌드가 `.next/server/middleware-manifest.json` 누락 후 재시도에서 `.next/trace` EPERM으로 중단됐다.
- 원인: 작업 폴더에서 실행 중인 다른 Node/Next 프로세스가 동일한 `.next` 빌드 디렉터리를 점유했다.
- 조치: 사용자 실행 프로세스는 종료하지 않았다. `npm.cmd run typecheck`와 `git diff --check`로 소스 검증을 완료하고, 격리된 Vercel clean build에서 최종 확인하도록 남겼다.

## [2026-07-21] 연결 승인 후 학생 홈 프로필 없음 오류

- 증상: 선생님 연결 승인 후 학생 홈에 `Student profile: no data returned.`와 학생 정보를 불러오지 못했다는 안내가 표시됐다.
- 확인: 최근 승인 요청의 `students` 행, Auth 사용자, `invited_by_teacher_id`가 모두 정상적으로 존재했다.
- 원인: 연결 링크 처리 과정에서 같은 브라우저가 선생님 계정으로 로그인되어 있었고, 그 선생님 Auth ID로 학생 홈 API를 호출해 대응하는 학생 프로필이 없었다.
- 조치: 학생 홈에서 먼저 현재 계정 역할을 확인한다. 선생님 세션이면 관리자 대시보드로 이동하고, 학생 프로필이 없는 계정에는 학생 계정 재로그인을 명확히 안내한다.

## [2026-07-22] 로그인 역할별 조직 데이터 미동기화

- 증상: 하위 선생님에게 반·공지·관리자 프로필이 보이지 않고, 학생 마이페이지·반 선택·랭킹 이벤트가 비어 있으며 칭찬 요청이 관리자 승인함에 나타나지 않았다.
- 원인: 전역 AppStore가 빈 mock seed로 시작했고 학생 홈만 별도 API 데이터를 사용했다. 반·공지·이벤트 생성 및 학생 출석·숙제·칭찬 요청도 대부분 브라우저 메모리에만 저장됐다.
- 확인: 실제 Supabase 조직에는 선생님 2명, 학생 3명, 기본반 1개만 존재했고 notices/reward_campaigns/homework_submissions/praise_requests는 0건이었다.
- 조치: `/api/app-state` 전역 hydrate, `/api/student/actions`, `/api/admin/approvals`, `/api/app-mutations`를 추가해 조직 데이터 조회와 변경을 Supabase에 연결했다.
- 참고: 과거 브라우저 메모리에만 만들고 DB에 저장되지 않은 특강반·공지·이벤트는 서버에서 복구할 수 없으므로 배포 후 다시 등록해야 한다.

## [2026-07-22] 연결 해지가 기본반 승인까지 취소하는 오류

- 증상: 전에 연결 승인된 학생이 학생관리에서 다시 승인 대기로 표시되고, 학생 로그인 시 선생님 연결 카드가 재노출되며 기본반도 사라졌다.
- 원인: 학생관리의 `disconnect` 처리가 `invited_by_teacher_id`만 해제하지 않고 승인된 enrollments까지 `rejected`로 변경했다.
- 확인: 두 학생이 실제 DB에서 `invited_by_teacher_id = null`, 기본반 `rejected`, 연결 요청 `revoked/pending` 상태였다.
- 조치: 연결 해지는 반 등록 상태를 변경하지 않도록 수정했다. 두 학생의 선생님 연결과 기본반 승인을 실제 DB에서 복원하고, 연결/반 상태가 모두 정상임을 재확인했다.
## [2026-07-22] 기본반 이벤트가 진행중으로 표시되지 않음

- 증상: 학생이 기본반 소속이고 관리자가 이벤트를 생성했지만 학생 화면에는 진행중 이벤트가 없다고 표시됐다.
- 원인: 이벤트 상태 계산 기준일이 `2026-07-19`로 고정되어 있어 그 이후 시작 이벤트가 실제 날짜와 무관하게 예정 상태로 분류됐다. 또한 이벤트 생성 화면에서 기본반을 명시적으로 선택할 수 없었다.
- 조치: 상태 계산을 실제 현재 시각 기준으로 변경하고 이벤트 생성 대상의 기본값에 `기본반 (상시)`를 추가했다. 모든 기존·신규 학생의 기본반 enrollment를 approved로 유지하는 보정 마이그레이션도 추가했다.
## [2026-07-22] 학생 오류 화면에서 로그인 진입 시 500/재이동

- 증상: 학생 데이터 로드 오류 후 `로그인으로 이동`을 누르면 로그인 화면에서 500이 발생하거나 기존 화면으로 다시 이동했다.
- 원인: 오류를 일으킨 기존 Supabase 세션을 유지한 채 `/login`으로 이동해 로그인 폼의 자동 프로필 조회·역할별 리디렉션이 다시 실행됐다. 로그인 쿼리 파라미터도 단일 문자열만 가정하고 있었다.
- 조치: 오류 복구 진입에 `reauth=1`을 사용해 로컬 세션을 정리하고, 로그인 페이지의 모든 쿼리 파라미터를 문자열 여부 확인 후 사용하도록 변경했다.
## [2026-07-22] 출석·과제 반복 체크로 중복 지급 가능

- 증상: 같은 학생이 같은 반에서 출석 또는 과제 체크 버튼을 당일 여러 번 눌러 스티커를 반복 지급받을 수 있었다.
- 원인: 화면과 API에 당일 기록 확인이 없었고 DB에도 학생·반·날짜 조합을 제한하는 제약이 없었다.
- 조치: Asia/Seoul 날짜 키를 저장하고 서버 사전 검사와 DB 부분 고유 인덱스를 함께 적용했다. 화면에서도 오늘 기록을 감지해 체크 버튼을 비활성화한다.

## [2026-07-22] 특강반 승인 후 학생이 다시 승인 대기로 표시됨

- 증상: 관리자가 승인한 학생이 승인 신청자 목록에 반복해서 나타났다.
- 원인: 특강반 신청 enrollment를 `pending`으로 저장했고 학생관리 API가 pending enrollment까지 선생님 연결 대기로 합산했다.
- 조치: 특강반은 신청 즉시 `approved`로 저장하고, 연결 대기는 `student_connection_requests`만 기준으로 판단하도록 분리했다.

## [2026-07-22] 로컬 빌드 명령 실행 오류

- 증상: PowerShell에서 정규식의 파이프 문자와 `npm.ps1` 실행 정책 때문에 첫 빌드 명령이 실행되지 않았다.
- 원인: PowerShell 문자열 해석과 스크립트 실행 정책이었다.
- 조치: 검색 문자열을 작은따옴표로 감싸고 `npm.cmd run build`로 실행해 프로덕션 빌드를 완료했다.
## [2026-07-22] 빌드와 타입 검사 병렬 실행 시 `.next/types` 충돌

- 증상: `npm.cmd run build`와 `npm.cmd run typecheck`를 동시에 실행했을 때 타입 검사에서 `.next/types/app/...` 파일을 찾지 못한다는 `TS6053` 오류가 발생했다.
- 원인: Next.js 빌드가 `.next/types`를 재생성하는 동안 TypeScript가 같은 디렉터리를 읽어 일시적인 파일 경쟁 상태가 발생했다.
- 조치: 프로덕션 빌드를 먼저 완료한 뒤 타입 검사를 순차 실행했고 두 검증 모두 통과했다. 이후 두 명령은 병렬 실행하지 않는다.

## [2026-07-22] 과제 완료 후 처리 중 상태가 유지됨

- 증상: 과제를 체크하고 스티커가 지급돼도 과제 내역에는 `처리 중`이 계속 표시됐다.
- 원인: 신규 API는 즉시 승인으로 변경됐지만 이전 배포에서 생성된 `pending` 과제 행과 누락 원장이 데이터베이스에 남아 있었다.
- 조치: 앱 상태를 불러올 때 pending 과제를 승인으로 전환하고 해당 과제의 스티커 원장이 없을 때만 보정 생성하도록 했다.

## [2026-07-22] 학생 프로필 사진 변경 버튼이 동작하지 않음

- 증상: 학생 마이페이지에서 `사진 변경`을 눌러도 사진 선택이나 촬영 화면이 열리지 않고 안내 문구만 표시됐다.
- 원인: 버튼이 실제 파일 입력이나 저장 API에 연결되지 않은 임시 안내용 구현이었다.
- 조치: 앨범 선택·카메라 촬영 입력을 추가하고 선택 이미지를 512px 정방형으로 축소한 뒤 학생 프로필 API를 통해 저장하도록 연결했다.

## [2026-07-22] 학생 이벤트 경품이 잠시 빈 목록으로 표시됨

- 증상: 관리자가 경품 리스트를 등록했어도 학생이 이벤트 경품 탭을 처음 열면 `등록된 이벤트 경품이 없습니다.`가 먼저 나오고 뒤늦게 상품이 표시됐다.
- 원인: 경품 API 요청을 전체 이벤트 화면을 연 뒤 시작했고, 요청 완료 여부와 실제 빈 목록을 구분하지 않은 채 초기 빈 배열을 즉시 렌더링했다.
- 조치: 학생 홈 진입 시 경품을 선조회하고 별도의 로딩 상태와 스켈레톤을 추가했다. 좋아요 테이블 미적용 환경에서도 기존 경품 조회는 실패하지 않도록 했다.

## [2026-07-22] 변경한 학생 프로필 사진이 홈·랭킹에 반영되지 않음

- 증상: 마이페이지 아바타에는 변경한 사진이 표시되지만 학생 홈 상단과 랭킹 아바타는 계속 이름 첫 글자로 표시됐다.
- 원인: 학생 홈과 랭킹 데이터에는 `profile_image_url`이 이미 포함되어 있었지만 해당 값을 공용 `Avatar` 컴포넌트에 전달하지 않았다.
- 조치: 홈의 현재 학생과 랭킹의 각 학생 프로필에서 사진 주소를 찾아 `Avatar`의 `imageUrl`로 전달하도록 연결했다.

## [2026-07-22] 이벤트 생성에서 등록 경품을 선택할 수 없음

- 증상: 경품 리스트에 상품이 등록되어 있어도 이벤트 생성의 상품 선택 목록이 비어 있고 경품 관리 페이지를 방문한 뒤에야 나타났다.
- 원인: 이벤트 생성은 전역 `productCatalog`를 참조했지만 최초 `/api/app-state` 응답에는 상품 데이터가 포함되지 않아 경품 관리 페이지의 별도 API 호출 전까지 빈 배열로 남았다.
- 조치: 최초 앱 상태 응답에 상품 목록을 포함하고 사용자별 캐시 복원, 이벤트 화면의 비파괴 백그라운드 갱신과 수동 새로고침을 추가했다.
## [2026-07-22] 상품정보 자동입력 버튼을 눌러도 폼이 바뀌지 않음

- 증상: 개발자 상품 등록 화면에서 상품정보 자동입력을 눌러도 상품명이나 가격이 바뀌지 않고 실패 여부도 분명히 알 수 없었다.
- 원인: 화면이 상품명과 설명의 기존 값이 비어 있을 때만 분석값을 반영했고, 링크 분석 API는 쇼핑몰이 봇 요청을 차단하거나 동적 페이지를 반환해 모든 분석값이 비어도 성공 응답을 반환했다.
- 조치: 분석값이 있으면 기존 폼 값을 갱신하고, 브라우저 요청 헤더와 구조화 데이터 분석을 보강했다. 찾은 항목을 화면에 표시하며 아무 항목도 없으면 명시적인 실패 안내를 반환하도록 변경했다.
## [2026-07-22] 테무 정보 오인식 및 쿠팡 링크 자동입력 실패

- 증상: 테무 상품에서 다른 상품명·가격·이미지가 입력되고, 쿠팡 일반 상품 링크는 `상품 페이지를 열 수 없습니다.`로 실패했다.
- 원인: 테무 페이지의 공용 스크립트에는 상품과 무관한 여러 `price`·`image` 키가 있어 범용 정규식이 첫 값을 오인했다. 쿠팡은 일반 상품 페이지의 서버 접근을 제한하며, 파트너스 iframe은 최종 HTML이 아니라 중간 리다이렉트 URL에 상품명·이미지·구매 링크를 제공한다. 해당 iframe에는 가격 값이 없다.
- 조치: 테무의 느슨한 스크립트 검색을 중단하고 검증된 상품 메타데이터만 사용했다. 쿠팡 iframe 태그의 `src`와 리다이렉트 쿼리를 분석해 상품명·이미지·실제 링크를 채우고, 제공되지 않는 가격은 직접 입력하도록 명시했다.
## [2026-07-22] 랭킹 기준일이 하루 전이고 당일 스티커가 0장으로 표시됨

- 증상: 실제 한국 날짜는 22일인데 일간 랭킹 기간이 21일로 표시되고, 22일에 받은 스티커가 전체 랭킹에서 0장으로 집계됐다.
- 원인: 한국 시간 자정의 `Date`를 `toISOString()`으로 변환해 UTC 날짜를 잘랐기 때문에 날짜가 21일로 밀렸다. 원장 역시 UTC `created_at`의 앞 10자리만 잘라 기간과 비교했다.
- 조치: 기간 경계와 각 원장 날짜를 모두 `Asia/Seoul` 기준 날짜로 변환해 비교하도록 수정했다. 그룹 집계는 해당 반 `class_id`의 원장만 포함하며, 기간 종료일 이후에 받은 스티커는 이전 기간 그룹 합계에 포함하지 않는다.
## [2026-07-22] 생년월일 전환 후 레거시 온보딩 API 타입 오류

- 증상: 학생 가입 모델을 `age`에서 `birthDate`로 변경한 뒤 타입 검사에서 레거시 `/api/onboarding/student`가 더 이상 존재하지 않는 `age` 값을 전달한다는 오류가 발생했다.
- 원인: 현재 가입 화면은 `/api/signup`을 사용하지만 이전 온보딩 호환 API도 같은 저장소 입력 타입을 공유하고 있었다.
- 조치: 레거시 온보딩 API도 생년월일을 검증하고 `birthDate`를 전달하도록 함께 변경했다.
## [2026-07-22] 쿠팡 iframe 태그 입력 후 올바른 구매 링크 오류로 등록 불가

- 증상: 쿠팡 파트너스 iframe 태그를 붙여넣었지만 서버 자동조회가 차단되면서 태그 원문이 링크 입력란에 남고, 상품 등록 시 URL 검사를 통과하지 못했다.
- 원인: iframe의 `src` 추출을 서버 자동조회 성공 응답에만 의존해, 쿠팡이 Vercel 요청을 차단하면 구매 링크 정규화도 함께 건너뛰었다.
- 조치: 브라우저에서 iframe `src`를 먼저 추출하고 자동조회 성공 여부와 무관하게 저장 링크로 사용하도록 분리했다.
## [2026-07-22] 학생 이벤트 경품의 이미지가 깨진 아이콘으로 표시됨

- 증상: 일부 외부 상품 이미지가 학생 이벤트 경품 목록에서 깨진 이미지 아이콘으로 표시됐다.
- 원인: 외부 쇼핑몰 이미지가 만료되거나 핫링크를 차단할 수 있는데, 실패 시 지정한 `/images/placeholder-product.svg` 파일도 프로젝트에 존재하지 않았다.
- 조치: 실제 기본 경품 SVG를 추가하고 이미지 `onError`에서 재시도를 막은 뒤 기본 이미지로 교체하도록 했다.
## [2026-07-22] 개발자 상품 수정이 관리자 경품 리스트에 반영되지 않음

- 증상: 개발자가 원본 상품명이나 가격, 이미지 등을 수정해도 이미 경품 리스트에 담은 관리자 상품은 이전 값으로 남았다.
- 원인: 추천 상품을 담을 때 `product_catalog`에 사본을 만들지만, 이후 `marketplace_products` 수정 시 `source_marketplace_product_id`로 연결된 사본을 갱신하는 처리가 없었다.
- 조치: 개발자 상품 PATCH 성공 후 연결된 모든 `product_catalog` 행을 함께 갱신하고, 데이터 혼선을 막기 위해 연동 상품의 관리자 PATCH를 서버에서 거부했다.
## [2026-07-22] 직접 등록한 상품 이미지가 다른 화면에서 깨짐

- 증상: 로컬 파일을 선택해 미리보기에서는 보이지만 상품 목록이나 학생 경품 화면에서는 이미지가 깨지거나 대체 이미지로 표시됐다.
- 원인: 파일을 실제 저장소에 업로드하지 않고 전체 이미지를 Base64 data URL로 변환해 상품 레코드에 직접 저장했다. 큰 이미지에서는 요청·응답 크기가 급증하고 여러 API를 거치는 과정에서 안정적인 영구 이미지 주소로 사용할 수 없었다.
- 조치: 인증된 개발자·관리자 전용 이미지 업로드 API를 추가하고, `product-images` 공개 버킷에 파일을 저장한 뒤 공개 URL만 상품 데이터에 기록하도록 변경했다.
## [2026-07-22] 추천상품을 경품 리스트에 담으면 이미지가 깨짐

- 증상: 추천상품에서는 보이던 이미지가 경품 리스트나 학생 이벤트 경품으로 복사된 뒤 깨졌다.
- 원인: 외부 쇼핑몰의 일시적·접근 제한 이미지 URL 또는 과거 Base64 이미지 문자열을 그대로 `product_catalog`에 복사했다.
- 조치: 경품 리스트 저장 시 이미지를 `product-images` Storage로 복제하고 공개 URL을 기록했다. 기존 비저장소 이미지도 관리자 상품 조회 시 한 번 복구해 DB 값을 갱신하도록 했다.

## [2026-07-22] 그룹 이벤트 당첨 순위가 학생 화면에서 실제 인원과 다를 수 있음

- 증상: 서버의 경품 선택 순서와 학생 화면에 표시되는 순위·선택 가능 상태가 달라질 수 있었다.
- 원인: 학생 홈 응답에는 현재 학생의 소속 정보만 포함되어 그룹 랭킹 계산에서 같은 반의 다른 학생이 제외됐다.
- 조치: 학생 홈 데이터에는 학원 전체 승인 소속 정보를 제공하되 화면에 표시하는 반은 현재 학생 소속 반으로 제한해, 서버와 화면이 같은 순위 대상을 계산하도록 했다.
## [2026-07-22] 일부 추천상품만 경품 리스트와 학생 앱에서 이미지가 사라짐

- 증상: 추천상품 카드에서는 이미지가 보이지만 경품 리스트에 담은 뒤 관리자와 학생 화면에서 일부 상품 이미지만 비어 있거나 깨졌다.
- 원인: `prize_image_url ?? image_url` 선택 방식이 빈 문자열도 유효한 값으로 취급했다. 경품 이미지 입력이 비어 있으면 실제 구매 이미지가 있어도 빈 문자열이 `product_catalog.image_url`로 복사됐다. 또한 기존 복구는 이미지 값이 있는 행만 검사하고 관리자 조회 때만 실행했다.
- 조치: 공백 제거 후 `경품 이미지 → 구매 이미지` 순서로 실제 값만 선택하고, 연결 원본까지 조회하는 공통 복구 함수를 관리자·학생 API 모두에 적용했다. 기존 DB 행과 생성된 이벤트 상품 이미지도 SQL 마이그레이션으로 보정했다.
## [2026-07-22] 개발자 상품 수정 후 기존 이벤트와 학생 경품 화면이 갱신되지 않음

- 증상: 개발자가 상품명이나 경품 이미지를 수정해도 관리자 경품 리스트 일부와 이미 생성된 이벤트, 학생 경품 화면에는 이전 값이 남았다.
- 원인: 추천 상품을 경품 리스트와 이벤트에 담을 때 값을 복사해 저장했지만, 이후 원본 `marketplace_products` 수정은 `product_catalog`까지만 갱신했고 `reward_items` 복사본은 갱신하지 않았다. 조회 화면도 복사본을 그대로 사용했다.
- 조치: 개발자 상품 수정 시 `marketplace_products → product_catalog → reward_items` 순서로 연결된 데이터를 갱신하고, 관리자·학생 조회에서는 `product_id`로 연결된 최신 카탈로그 값을 우선 합성하도록 수정했다. 이벤트 수정 시에도 연결 상품 정보를 다시 동기화한다.
- 추가 조치: 교체 이미지가 브라우저의 장기 캐시에 남지 않도록 개발자 수정 때마다 새 버전의 Storage 경로를 발급해 URL 자체가 변경되도록 했다.

## [2026-07-22] PowerShell에서 npm 빌드 명령 실행 실패

- 증상: `npm run build` 실행 시 PowerShell 실행 정책 때문에 `npm.ps1`을 로드할 수 없었다.
- 원인: 시스템의 PowerShell 스크립트 실행 정책이 npm PowerShell 래퍼 실행을 차단했다.
- 조치: 동일한 Node 설치의 `npm.cmd run build`로 실행해 전체 빌드와 타입 검사를 완료했다.
## [2026-07-22] 관리자 반별 랭킹에서 소속 반이 여러 개 표시됨

- 증상: 기본반이나 특강반 랭킹을 선택해도 랭킹 표의 소속 반 열에는 해당 학생이 가입한 모든 반이 표시되어 집계 범위가 불분명해 보였다.
- 원인: 스티커 계산은 이미 선택한 `class_id`로 제한하고 있었지만, 표의 소속 반 문구는 선택 범위와 관계없이 학생의 전체 승인 소속을 나열했다.
- 조치: 반별 필터에서는 선택한 반만 소속 반 열에 표시하도록 변경했다. 랭킹 계산은 선택한 반의 승인 학생과 해당 반의 원장 기록 및 선택 기간을 모두 조건으로 유지했다.
## [2026-07-22] 관리자 롤백 후 학생 스티커 누적 수량이 줄지 않음

- 증상: 관리자가 스티커 지급 기록을 롤백하면 관리자 화면에서는 취소로 바뀌지만 학생 스티커 탭의 전체 누적 개수는 그대로였다.
- 원인: `ROLLBACK_LEDGER` 동작이 클라이언트 상태만 변경하고 서버 저장 대상 목록과 저장 API 분기에 포함되지 않았다. 또한 학생 스티커 탭은 앱 최초 접속 때 받은 상태를 계속 사용해 탭 진입 시 최신 원장을 다시 확인하지 않았다.
- 조치: 롤백을 서버 `sticker_ledger`에 영구 저장하고, 학생 스티커 탭 진입·포커스 복귀 시 최신 앱 상태를 다시 받아오도록 했다. 기존 합계 계산의 `status === active` 조건으로 취소 기록이 자동 제외된다.
## [2026-07-22] 반별 출석으로 특강 수강 수만큼 출석 스티커를 받을 수 있음

- 증상: 학생이 여러 특강반에 소속되면 반을 바꿔가며 같은 날 출석 스티커를 여러 번 받을 수 있었고, 반별 랭킹마다 출석·칭찬 점수의 의미가 달라졌다.
- 원인: 출석 중복 제한이 `(student_id, class_id, check_date)` 기준이고 출석·칭찬 원장에도 선택한 반을 기록했다.
- 조치: 출석 중복 기준을 `(student_id, check_date)`로 변경하고 정규반 원장에 하루 한 번만 저장한다. 칭찬도 반 선택을 제거해 하루 한 번만 요청하며, 랭킹 계산에서 출석·칭찬은 공통 점수로, 과제만 선택한 특강반 점수로 처리한다.

## [2026-07-22] 경품 선택 API 삭제 후 타입 검사에서 이전 경로를 찾음

- 증상: 경품 선택 API 파일을 삭제한 직후 타입 검사에서 `.next/types/app/api/student/reward-claims/route.ts`가 삭제된 원본 모듈을 찾지 못했다.
- 원인: 이전 빌드에서 생성된 Next.js 타입 캐시에 삭제 전 API 경로가 남아 있었다.
- 조치: 해당 API 경로의 생성 타입 캐시만 제거한 뒤 타입 검사와 전체 프로덕션 빌드를 다시 실행해 통과했다.
## [2026-07-23] 소유자 선생님 삭제 시 tenants 외래키 오류

- 증상: 선생님 행을 삭제할 때 `tenants_owner_teacher_id_fkey` 때문에 `teachers.id`가 `tenants.owner_teacher_id`에서 참조 중이라는 오류가 발생했다.
- 원인: 해당 외래키에 삭제 동작이 지정되지 않아 PostgreSQL 기본값인 `NO ACTION`이 적용됐다.
- 조치: 외래키를 다시 생성하면서 `ON DELETE SET NULL`을 지정했다. 이제 선생님 삭제 시 학원 행은 보존되고 소유자 참조만 자동으로 비워진다.
## [2026-07-23] 새 출석 공식에서도 과거 반별 출석이 중복 합산될 수 있음

- 증상: 새 출석 정책은 하루 한 번이지만, 정책 변경 전에 여러 반에서 받은 같은 날 출석 원장이 활성 상태로 남아 전체 및 특강반 랭킹에 여러 번 더해질 수 있었다.
- 원인: 반별 랭킹이 출석을 공통 점수로 포함하도록 바뀐 뒤에도 과거 `sticker_ledger`의 반별 출석 행을 그대로 모두 합산했다.
- 조치: 활성 원장 집계 전에 출석을 학생별·한국 날짜별 첫 한 건으로 정규화했다. 전체 합계, 일별 상세, 랭킹, 관리자 학생 총합에 같은 함수를 사용하고, 운영 DB의 후속 중복 출석은 별도 마이그레이션으로 롤백 처리한다.

## [2026-07-23] 로컬 Prettier 실행 시 npm 캐시 EPERM

- 증상: 설치되지 않은 Prettier를 `npm exec`로 실행하면서 사용자 npm 캐시에 쓰지 못해 EPERM 오류가 발생했다.
- 원인: 작업공간 밖의 npm 캐시가 현재 실행 권한 범위에 포함되지 않았고 패키지 다운로드가 필요했다.
- 조치: 외부 패키지를 추가하지 않고 기존 파일 형식을 유지한 채 `apply_patch`로 필요한 부분만 수정했다.
